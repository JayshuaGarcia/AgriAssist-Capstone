{
  "name": "agriassist-pdf-api",
  "version": "1.0.0",
  "description": "PDF fetching API for AgriAssist app",
  "scripts": {
    "start": "node scripts/renderPDFService.js",
    "dev": "node scripts/renderPDFService.js",
    "build": "echo 'No build step required'",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}