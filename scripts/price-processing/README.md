# Price Processing Scripts

This folder contains Python processing code for price data:
- Python scripts for processing CSV files
- Data transformation scripts
- Forecast generation scripts

## Setup
1. Ensure Python is installed
2. Install dependencies: `pip install -r requirements.txt` (if needed)
3. Run scripts as needed

